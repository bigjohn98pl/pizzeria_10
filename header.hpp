#ifndef HEADER_HPP
#define HEADER_HPP


enum length{
    NAME = 22,
    PRICE = 10,
    INGREDIENTS = 40,
    SIZE = 5,
    SPACE = 4
};
#include <iostream>
#include <iomanip>
#include <fstream>
#include <string>
#include <vector>
#include <sstream>

using namespace std;

#endif // HEADER_HPP
